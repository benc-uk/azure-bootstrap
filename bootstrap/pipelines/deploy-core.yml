#
# 
#

trigger: none

variables:
  azureConnection: "azure"

jobs:
- template: templates/terraform-apply.yaml
  parameters:
    name: macOS
    azureConnectionName: $(azureConnection)
    terraformDir: bootstrap/terraform/core
    varFile: bootstrap/vars.tfvars