#
# 
#

trigger: none

variables:
  azureConnection: "azure"

jobs:
- template: /templates/terraform-apply.yaml
  parameters:
    name: macOS
    azureConnectionName: $(azureConnection)
    terraformDir: $(System.DefaultWorkingDirectory)/bootstrap/terraform/core
    varFile: $(System.DefaultWorkingDirectory)/bootstrap/vars.tfvars